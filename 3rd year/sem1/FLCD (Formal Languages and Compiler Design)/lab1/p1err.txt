let 
    a: int := 10;
    b: int := 15;
    c: int := 8;
    1max: int := a;  // A variable must start with a letter

if b > max then     // 'end' expected
    max := b;


if c > max then
    max := c;
end

output(max);
end
